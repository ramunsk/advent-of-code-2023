{
    "configurations": [
        {
            "name": "Debug current Jest suite",
            "type": "node",
            "request": "launch",
            "program": "${workspaceRoot}/node_modules/jest/bin/jest.js",
            "args": ["-i", "--runInBand", "--watchAll=false", "${relativeFile}"],

            "internalConsoleOptions": "neverOpen"
        }
    ]
}
